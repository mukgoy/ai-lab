import { BotuiFocusDirective } from './botui-focus.directive';

describe('BotuiFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new BotuiFocusDirective();
    expect(directive).toBeTruthy();
  });
});
