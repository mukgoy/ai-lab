import { BotuiScrollDirective } from './botui-scroll.directive';

describe('BotuiScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new BotuiScrollDirective();
    expect(directive).toBeTruthy();
  });
});
